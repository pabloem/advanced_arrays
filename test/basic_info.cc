#include <stdio.h>
#include <cstdlib>

int main(){
  printf("The size of int datatype is: %d\n",sizeof(int));
  printf("The size of short datatype is: %d\n",sizeof(short));
  printf("The size of void* datatype is: %d\n",sizeof(void*));
}
